'use client';

import styles from 'styled-components';
import { metallicGray } from '@amn/styles/Colors';

export default styles.form`
eui-input {
  label {
    color: ${metallicGray};
  }
}
`;
