apiVersion: v1
kind: Service
metadata:
  name: amn-frontend
spec:
  type: LoadBalancer
  selector:
    app: amn-frontend
  ports:
    - port: 80
      # The port in which the application is running inside the container/pod (the one configured in the env variables)
      targetPort: 3000
      nodePort: 30080